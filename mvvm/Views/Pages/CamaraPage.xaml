<ui:UiPage x:Class="mvvm.Views.Pages.CamaraPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:mvvm.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="CamaraPage"
    d:DataContext="{d:DesignInstance local:CamaraPage, IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    d:Background="{DynamicResource ApplicationBackgroundBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="Height" Value="20"/>
                <Setter Property="Width" Value="60"/>
            </Style>
        </Grid.Resources>
        <Button Grid.Row="0" Command="{Binding ViewModel.CloseCommand}">Close</Button>
        <Image Grid.Row="1" Source="{Binding ViewModel.CurrentFrame}" Name="imgViewport" Stretch="Uniform" Visibility="Visible"/>
        <MediaElement Grid.Row="1" Name="videoViewport" Stretch="Uniform" Visibility="Collapsed"/>
    </Grid>
</ui:UiPage>