<Page x:Class="insoles.CamaraViewport.CamaraViewport"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:insoles.CamaraViewport"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="{Binding title}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="Height" Value="20"/>
                <Setter Property="Width" Value="60"/>
            </Style>
        </Grid.Resources>
        <Grid Grid.Row="0">
            <Button Click="onClose" HorizontalAlignment="Center">Close</Button>
            <CheckBox x:Name="record" HorizontalAlignment="Right" Padding="0 0 50 0">Record</CheckBox>
        </Grid>
        <Image Grid.Row="1" Name="imgViewport" Stretch="Uniform" Visibility="Visible"/>
        <MediaElement Grid.Row="1" Name="videoViewport" Stretch="Uniform" Visibility="Collapsed"/>
    </Grid>
</Page>
